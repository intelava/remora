# Required for core tensor operations and GPU acceleration
torch

# Required for custom high-performance GPU kernels
# This requires a Linux environment with an NVIDIA GPU.
triton

# Used in the demo for performance metric calculations
numpy

# Required for the Atari environment
gymnasium[atari]
ale-py
opencv-python-headless

# Required for the Moondream2 VLM
transformers
timm
einops
sentencepiece
Pillow